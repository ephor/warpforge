/**
 * Stands in for the app's Files surface on the marketing site.
 *
 * Same bargain as the terminal stub: the demo stays on the Diff, Runtime and
 * Pipeline tabs, and the real editor drags in all of CodeMirror plus the LSP
 * client. Aliased in `astro.config.ts`.
 */
export function FilesSurface() {
  return null;
}
export default FilesSurface;
