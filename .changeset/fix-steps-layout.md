---
"warpforge": patch
---

Numbered step lists in the documentation render properly again. Any step written as bold text followed by an ordinary sentence had that sentence squeezed into the width of its number circle and printed one letter per line, which affected every set of steps on the site.
