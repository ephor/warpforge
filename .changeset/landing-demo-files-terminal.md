---
"warpforge": patch
---

The demo on the website's front page is now a whole workspace, not just a conversation and a diff. The Files tab opens the real editor on a real project tree — syntax highlighting, folding, the changed files marked — and the Terminal tab shows a session with the project's services on their allocated ports and its test suite passing. Both are there to click into while the run plays.
