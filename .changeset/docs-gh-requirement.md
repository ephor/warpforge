---
"warpforge": patch
---

The install docs now say what the GitHub CLI is actually for. It is not just **Open pull request** — the GitHub backlog reads your issues through the same `gh` session, and uses it again to load attachments on private ones. Commit and push still go straight through git and need nothing extra.
