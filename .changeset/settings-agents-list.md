---
"warpforge-desktop": patch
---

The agent picker is a list instead of a two-column grid of boxes, matching every other list in Settings. Each agent is one row with its version, command and model count, so the section is a single column you scan top to bottom rather than a chessboard. The same list is used when you first set up agents, where two columns never fit the dialog properly anyway.
