---
"warpforge": patch
---

Fix memory for agents: saving no longer fails after deleting a memory, and search now finds notes by tags and partial terms instead of returning empty results. Existing databases migrate automatically.
