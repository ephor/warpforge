---
"warpforge": patch
---

Warpforge now has a dedicated documentation site, so install steps, agent setup, orchestration and workflow guides, the full workspace-config reference, and the CLI reference live outside the README instead of one long scroll. The README stays focused on the pitch and a quick install.
