---
"warpforge": minor
---

Browse a project's code without starting a task. The project page has a Files tab that opens the project's own checkout: pick anything in the tree on the right and it opens in a full syntax-highlighted preview, with the same collapse control the task workspace uses. The preview is read-only on purpose — edits belong to a task, so nothing you open here can be changed by accident.
