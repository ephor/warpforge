---
"warpforge": patch
---

Documentation now covers the day-to-day details of working inside a task: sending a file, a diff, or a highlighted selection straight to the chat, dragging files from the tree into the composer, going to a definition, reading live diagnostics from a language server, letting Enhance turn a rough note into a properly scoped task, and the commit, pull-request and title text that gets drafted for you. Every keyboard shortcut is listed in one table.
