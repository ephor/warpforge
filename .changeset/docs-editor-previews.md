---
"warpforge": patch
---

Documentation now covers the editor's previews — Markdown rendered or as source, HTML pages rendered so you can look at a prototype instead of reading it, SVG either way, and images shown directly — along with the note that HTML previews are sandboxed, so script-driven pages need a real dev service. The new-task composer's run preview, which draws the actual stages of the workflow you picked before you press Start, is written down too.
