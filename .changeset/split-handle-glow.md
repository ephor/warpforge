---
"warpforge-desktop": patch
---

The divider between the conversation and the workspace is invisible until you reach for it. Instead of a permanent grey bar it stays out of the way, and lights up as a soft blurred band under the pointer — fading out at the top and bottom rather than butting into the chrome. The grab area is unchanged, so it is no harder to drag.
