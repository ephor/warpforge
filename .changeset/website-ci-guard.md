---
"warpforge": patch
---

The website's front-page demo is now checked on every change, so it cannot quietly stop working: each pull request builds the site and plays the demo run through, failing if the conversation, the diff or the changes rail does not arrive.
