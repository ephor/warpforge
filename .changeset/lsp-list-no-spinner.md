---
"warpforge-desktop": patch
---

The language server list opens instantly. It used to re-detect every server from scratch each time you opened that part of Settings, leaving you staring at a spinner; now the last known list appears right away and version checks happen quietly in the background. Refresh sits in one place instead of sliding across the panel while it works.
