---
"warpforge": patch
---

Backlog rows now read across one line instead of stacking onto two. Status, priority, tracker, assignee and last-changed each sit in their own column, so the whole list lines up down the page and roughly twice as many items fit on screen. Narrower windows drop the least useful columns first rather than wrapping, and the row actions keep a reserved slot so nothing shifts when they appear on hover.
